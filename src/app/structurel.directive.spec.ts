import { StructurelDirective } from './structurel.directive';

describe('StructurelDirective', () => {
  it('should create an instance', () => {
    const directive = new StructurelDirective();
    expect(directive).toBeTruthy();
  });
});
